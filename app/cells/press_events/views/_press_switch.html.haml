= custom_form_for profile, url: profile_profile_path, as: :profile do |f|
  #press-switch.switch-builder
    .toggle
      %label.checkbox
        = f.input_field :press_on, as: :boolean, class: 'boolean optional'
